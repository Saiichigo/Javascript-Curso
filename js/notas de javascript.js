notas de javascript

variable.pop() = se utiliza para borrar el ultimo valor del array.

array.splice(varible, 1 o 2 o cualquiera) = se le indica que borre el valor pasado por la variable y el numero funciona pa indicar de 
se borraran en adelante.

var variable = nomobre_del_array.join() = esto convierte un array en un string separado por textOverflow


var variable = varible_texto.split("aqui la condicion para splitear")
// ejemplo 					nombre_array = nombres.split(", ");

array.sort() = para ordenar un arreglo por oreden numerico o alfabetico

array.reverse() = para hacer que el array este al revez.

variable = arrya.some = para si hay numeros mayores a una condicion
// ejemplo busqueda2 = precios.some(precio => precio > 80); 